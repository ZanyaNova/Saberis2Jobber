# HOW TO USE: replace all the values to the right of the = sign on each line. The examples explain what to do.
# --- App Defaults Configuration ---
DEFAULT_MARKUP=.035 #What is the default catalog markup as a decimal (0.035 = 3.5%)

# --- Google Sheets Configuration ---
# You need to convert your Google service account JSON credentials into a single base64 string.
GOOGLE_CREDENTIALS_BASE64= You need to convert the JSON credentials into a single base64 string and paste it here, ask an AI how to do it
WORKBOOK_URL=https://docs.google.com/spreadsheets/d/YOUR_SHEET_ID/edit

# --- Jobber OAuth Configuration ---
JOBBER_CLIENT_ID= get from jobber app page
JOBBER_CLIENT_SECRET= get from jobber app page
JOBBER_REDIRECT_URI=http://localhost:8080/jobber/callback

# --- Saberis API Configuration ---
# This is your long-lived authentication token from the Saberis website.
SABERIS_AUTH_TOKEN= get from saberis website, under settings

# --- Flask Configuration ---
FLASK_SECRET_KEY="An Arbitrary Secret just put any few words here" 

# --- Logging ---
LOG_PRIORITY_THRESHOLD=1 # (0=Critical, 1=Error, 2=Warn, 3=Info, 4=Debug)